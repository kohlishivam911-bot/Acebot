# Server-side only. Nothing here reaches the browser.

# Anthropic key — injected by the /api/anthropic proxy so it never ships in the bundle.
ANTHROPIC_API_KEY=

# Model used to generate prompts (the generator, not the voice bot).
GENERATOR_MODEL=claude-sonnet-4-6

PORT=3000
NODE_ENV=production
